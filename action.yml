# action.yml
name: "Akash On Github Actions"
description: "Akash CLI Tools in GitHub. Publish a Project."
inputs:
  walletkey: # id of input
    description: "list of secrets to load"
    required: true
  walletkeypass: # id of input
    description: "list of secrets to load"
    required: true
outputs:
  cli-version: # id of output
    description: "The version of the CLI running"
  network:
    description: "The network that the job is attached to"
  network-version:
    description: "The version of the Akash Network"
  network-node:
    description: "The network RPC node the job is using for chain communicating"
run: |-
  echo "${{inputs.walletkey}}" > key.key
  echo "${{inputs.walletkeypass}}" | akash keys import AKASH_GROWTH_GROUP key.key
  rm key.key

# runs:
#   using: "docker"
#   image: "Dockerfile"
#   args:
#     - ${{ inputs.walletkey }}
#     - ${{ inputs.walletkeypass }}
